// Tailwind CSS v4 is configured through @tailwindcss/vite in vite.config.js.
// Keep PostCSS explicitly empty so Vite does not try to load Tailwind as a
// legacy PostCSS plugin.
export default {
  plugins: {},
}
